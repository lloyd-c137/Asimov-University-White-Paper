{
  "name": "asimov-university-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute asimov-university --local --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute asimov-university --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260317.1",
    "typescript": "^5.3.3",
    "wrangler": "^3.24.0"
  }
}
